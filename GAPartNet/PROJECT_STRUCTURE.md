# GAPartNet Submodule for Main Table Row 02

This directory contains the algorithm code used for the main-table row 02 pipeline.
Large assets, datasets, generated outputs, checkpoints, and compiled artifacts are intentionally excluded from git.

Expected local-only directories are kept with `.gitkeep` placeholders, including:

- `dataset/`
- `example_assets/`
- `manipulation/assets/`
- `manipulation/output/`
- `gapartnet/data/`
- `gapartnet/output/`
- `gapartnet/ckpt/`
